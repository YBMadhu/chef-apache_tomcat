# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overwritten.

<% @jmx_users.each do |id, attribs| %>
  <% next unless %w(readonly readwrite).include?(attribs['access']) && attribs['password'] %>
<%= id %> <%= attribs['password'] %>
<% end %>
